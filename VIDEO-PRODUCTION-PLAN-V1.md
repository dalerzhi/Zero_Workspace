# 视频制作方案 v1

创建时间：2026-03-21 08:55 +08:00

## 目标
先把“单动作成片模板”做稳，再扩展到整套儿童训练视频批量生成。

## 选型结论

### 一阶段（立即开做）
- `video-frames` / ffmpeg：用于拆帧、截片段、生成关键帧素材
- `ffmpeg-video-editor` / 原生 ffmpeg：用于拼接、转码、加字幕条、混音、导出
- Python 脚本：负责编排时间轴、字幕层、进度条、批量渲染

### 二阶段（确认风格后再上）
- `remotion-video-toolkit`
- 用途：把当前脚本化流程升级成模板化视频生产线，支持统一主题、批量换动作/文案/配音

### 当前不优先
- `image-to-video`
- 原因：当前已有真实动作参考视频，没必要先走静图转视频路线

## 生产路线

### Phase 1：做出单动作 MVP
目标：拿 `deadbug` 跑通完整模板。

成片结构：
1. 开场标题（动作名 + 训练目标）
2. 准备阶段（倒计时）
3. 主动作循环（次数/左右侧/节奏提示）
4. 口播引导（中文）
5. 背景音乐（轻量 ducking）
6. 收尾鼓励 + 完成提示

输出要求：
- 16:9 主版 MP4
- 能稳定复用到其他动作
- 文案、时长、次数、提示语可配置

### Phase 2：抽象模板
把 deadbug 的脚本抽象成：
- 输入：动作配置 JSON/YAML
- 素材：参考视频 / 配音文案 / BGM / 封面图
- 输出：标准成片

建议配置项：
- 动作名
- 总次数
- 每次时长
- 准备时长
- 左右切换规则
- 口播脚本
- cue 文案
- 配色主题

### Phase 3：扩展成整套训练
扩展到：
- inchworm
- greatstretch
- balance
- calf
- chest
- jump
- splitsquat
- sideplank

再生成：
- 单动作版
- 合集版
- 预告版 / preview

## 近期执行顺序

### 今天先做
1. 固化 `deadbug` 单动作模板结构
2. 抽配置，避免写死到脚本里
3. 统一输出目录与命名规则
4. 预留 9:16 竖版导出入口

### 接着做
5. 复制模板到第二个动作验证通用性
6. 再决定是否引入 Remotion 做模板层

## 判断标准
如果满足下面 3 条，就说明 Phase 1 成功：
- 换一套动作文案后，不用重写大量代码
- 能稳定导出至少 2 个不同动作的视频
- 输出风格统一，能继续批量化

## 下一步
- 先基于现有 `build_deadbug_sample_v3.py` 抽出通用模板脚本
- 然后做一个动作配置文件，跑出 v1 pipeline
