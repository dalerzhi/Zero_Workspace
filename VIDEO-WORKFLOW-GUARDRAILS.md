# 视频制作工作流护栏

创建时间：2026-03-22 01:07 +08:00

## 问题
当前视频制作流程存在典型失控点：
- 写完脚本就等结果
- 缺少中间检查点
- 没有超时异常处理
- 把基础 QA 留到最终用户验收

这会导致：
- 卡住不自知
- 修掉 A 又引入 B
- 渲染时间白白浪费
- 用户被迫承担基础检查

## 新工作流（立即生效）

### Phase 0：先拆任务，不直接整片渲染
先把视频拆成 4 个独立模块验证：
1. 素材清洗（是否有黑屏 / title / 原声污染）
2. TTS（文案、音色、自然度）
3. BGM（是否真的是正常音乐，不是嗡嗡声）
4. Overlay（字幕 / cue / 倒计时）

任一模块不过，不进入整片导出。

### Phase 1：小样先行
不直接导出全片，先做 10-15 秒 sample：
- sample-a: TTS only
- sample-b: BGM only
- sample-c: TTS + BGM 混音
- sample-d: 画面 + 字幕 + 混音

只有 sample 过关，才允许进入 full render。

### Phase 2：过程检查点
每次做视频必须有这些 checkpoint：
- Checkpoint A：素材检查完成
- Checkpoint B：TTS 小样通过
- Checkpoint C：BGM 小样通过
- Checkpoint D：字幕 / cue overlay 通过
- Checkpoint E：整片 QA 通过

### Phase 3：超时异常处理
若任一步超过预期：
- 小样 > 10 分钟还没结论
- 整片 > 20-30 分钟还没新产物

则立即切换到异常检查：
1. 当前有没有新文件产出
2. 卡在脚本、素材、TTS、BGM 还是 ffmpeg
3. 是质量问题还是执行问题
4. 需要缩小范围还是替换方案

不能默认“它还在正常运行”。

### Phase 4：交付前闸门
发用户前必须回答全是“是”：
- 有中文 TTS
- 有正常 BGM
- 有字幕/cue
- 无英文原声
- 无黑屏 / 白字污染
- 自己完整检查过

## deadbug 当前具体执行方式
接下来 deadbug 不再直接整片试错，而是：
1. 先做 BGM 小样（单独验收）
2. 再做 TTS + BGM 混音小样
3. 再回灌到 deadbug 正片
4. 最后做 full render + QA

## 原则
- 先证明确实可用，再放大到整片
- 先自己发现基础错误，不把基础 QA 丢给用户
- 超时不是“耐心等待”，而是“触发排障”
