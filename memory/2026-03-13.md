# 2026-03-13

## 飞书官方插件迁移与验证

- 已完成从旧版 `@openclaw/feishu` 切换到新版官方插件 `@larksuiteoapi/feishu-openclaw-plugin@2026.3.8`。
- 配置已切换为：
  - `plugins.entries.feishu.enabled = false`
  - `plugins.entries.feishu-openclaw-plugin.enabled = true`
  - `plugins.allow = ["feishu-openclaw-plugin"]`
- 迁移过程中确认：如果旧版插件未禁用，新版工具会直接报冲突错误。

## 飞书权限/授权排障结果

- 新版插件初始卡在应用权限检查，缺少 `application:application:self_manage`。
- 文档创建链路后续又补齐了一轮文档相关 scopes，并完成多次用户 OAuth 授权。
- 结论：新版插件是“按能力分批授权”的，很多用户态能力不是一次授权全通，而是用到对应能力时继续弹授权卡片。
- `feishu_oauth revoke` 可用于撤销当前用户授权并重新走授权流程。

## 已验证通过的飞书能力

- 文档创建 ✅
- 文档读取 ✅
- 文档更新（append）✅
- 文档搜索 / Wiki 搜索 ✅
- 查询当前用户信息 ✅
- 查询主日历 ✅
- 读取任务清单 ✅
- 搜索飞书消息 ✅
- 搜索飞书用户 ✅

## 已验证受限的飞书能力

- 机器人主动给未建立会话的联系人发私聊失败，`message send` 返回 HTTP 400。
- 判断：机器人更适合群聊场景、已建立会话场景，或需要对方先与机器人建立关系；不适合直接主动私聊陌生联系人。

## 邮件日报 cron 修复

- 已把“邮件日报”cron 改成 isolated agentTurn，且模型为 `bailian/qwen3.5-plus`。
- 当前 job 会生成日报后，直接把日报正文作为 delivery 投递到飞书，而不是依赖旧的 `.feishu-credentials` 发送链路。
- 旧的禁用版“邮件日报”job 仍在，但不会执行。

## 心跳与模型降耗调整

- 当前主会话 heartbeat 原本为每 30 分钟一次，且走 main session 默认模型 `openai-codex/gpt-5.4`，会吃主会话上下文，消耗偏高。
- 已按用户要求关闭 heartbeat：`agents.defaults.heartbeat.every = "0m"`。
- 已确认当前有效 cron：
  - 邮件日报：`bailian/qwen3.5-plus`
  - Zero Workspace Backup：已补切到 `bailian/qwen3.5-plus`
- 用户明确表示：**主会话默认模型不要切到 qwen3.5-plus**，因为工作能力太差；只接受把 heartbeat 关掉、cron 用便宜模型。

## 用户偏好（重要）

- 降本策略：
  - ✅ 关闭 heartbeat
  - ✅ cron 尽量用便宜模型
  - ❌ 不要把 main session 默认模型切到 `qwen3.5-plus`
- 对 `qwen3.5-plus` 的评价：便宜但“像个傻子”，工作能力明显不够，不适合作为默认主模型。
