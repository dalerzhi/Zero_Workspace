# 2026-03-12 - 周四

## 📝 重要变更

- 将 OpenClaw 默认模型切到 `openai-codex/gpt-5.4`
- 将 fallback 模型切到 `bailian/qwen3.5-plus`
- 将 `agents.defaults.subagents.model` 设为 `openai-codex/gpt-5.3-codex`
- 确认后续凡是需要动代码的任务，默认优先起子 agent 处理

## 🔧 故障排查

### 邮件日报 cron
- 找到投递失败根因：cron delivery 字段应使用 `to`，不是 `target`
- 已修复 `~/.openclaw/cron/jobs.json`
- 手动验证邮件抓取脚本可正常运行（18 封邮件 / 49 个附件）

### Gateway 自动恢复
- 增加 watchdog：`scripts/openclaw-gateway-watchdog.sh`
- 增加 LaunchAgent：`~/Library/LaunchAgents/com.zero.openclaw-gateway-watchdog.plist`
- 首版 watchdog 因 launchd 环境缺少 node 而失败，后改为使用绝对路径调用 node + OpenClaw CLI
- 修复后日志显示：`gateway healthy`

## ⚠️ 观察到的问题
- Codex 子 agent 多次报 `server_error`，属于模型后端瞬时错误，可能导致单次子任务失败或中断
- 当前需要对这类编码子任务增加重试/降级预期，不能默认认为一次就稳
