
## 2026-03-28 晚间补记

- 用户刚升级 OpenClaw 后，让我自查状态。结论：主会话/Gateway/Feishu/cron 都正常；heartbeat 当前实际开启，配置为每 30 分钟一次，模型是 `bailian/qwen3.5-plus`。`openclaw status` / `security audit` 对 `agents.defaults.imageGenerationModel` 报“Unrecognized key”，但 schema lookup 和 config.get 都显示该字段合法，判断更像 CLI 校验/兼容性 bug，不是配置真坏。
- 用户给了一台 IDC 虚拟机让我部署 3X-UI。关键事实：公网端口 `40001-40005` 分别映射到内网 `10001-10005`，最开始因把公网端口直接配到服务监听口导致走偏，后来修正为按内网端口部署。
- 这台 IDC 机器上最终收敛为：3X-UI 面板走 `40001 -> 10001`，并在机器内通过 nginx + 自签证书包了一层 HTTPS；x-ui 原始管理口收到了 loopback。入站方面按用户反馈收敛到“只保留一条最小可用节点”，因为此前出现“单条能用、两条不行”的情况。
- 当前这台 3X-UI 最终可用方案是：只保留一条 `VLESS + Reality + xtls-rprx-vision` 节点，通过公网 `40002` 提供服务；旧的多条入站方案已删掉。后续如果再碰这台机器，优先假设“单节点稳定优先于多节点”。
- 用户测试确认这条单节点 Reality 可用，并让我输出了 Clash Verge 的单节点 `proxies` 片段。
- 用户提出新任务：让我“学会打电话”，并要求以后能主动给他的手机号外呼。用户明确授权我自己去搜索、尝试、按 autoresearch 的方法推进；需要什么平台/API，他来注册/充值，我负责研究到打通为止。这是一个明确的工作偏好：遇到新能力缺口，先自己推进方案与 PoC，不要停在口头建议。
- 电话能力路线调研后的当前主线：面向中国大陆手机号外呼，优先走 **阿里云语音服务**，且 MVP 选型为 **公共模式 + `SingleCallByTts`**；Twilio 不适合作为中国大陆手机号外呼的主路径。
- 为电话能力已落地本地基础设施：创建了 `scripts/aliyun_voice_call.py`（阿里云语音外呼脚本骨架，支持 `SingleCallByTts` / `SingleCallByVoice` / 查询详情）和 `docs/aliyun-voice-call-mvp.md`（MVP 说明文档），并准备了 `.venv-aliyun-vms` SDK 环境。当前阻塞点在阿里云账号侧：企业实名认证、开通语音服务、资质/话术/模板审核、分钟包、AK/SK、`TTS_CODE`。
